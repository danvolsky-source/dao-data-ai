# Exclude Python backend folder to avoid conflict with Next.js API routes
/api
/backend
/data_collection
lib/supabase.ts

# These are handled by FastAPI backend instead
# All API requests should go to /api/* which routes to backend/main.py
